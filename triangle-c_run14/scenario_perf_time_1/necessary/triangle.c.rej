--- before: triangle.c
+++ after: triangle.c
@@ -40,6 +40,10 @@
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
+    if(a + b <= c)/*auto*/{
+     
+      return INVALID;
+    }/*auto*/
   }/*auto*/
   return SCALENE;
 }
