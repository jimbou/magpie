--- before: triangle.c
+++ after: triangle.c
@@ -8,14 +8,13 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  
 
   // Sort the sides so that a <= b <= c
-  if(a > b) {
-    tmp = a;
-    a = b;
-    b = tmp;
-  }
+  if(a == b && b == c)/*auto*/{
+   
+    return EQUILATERAL;
+  }/*auto*/
 
   if(a > c) {
     tmp = a;
@@ -40,6 +39,7 @@
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
+    b = c;
   }/*auto*/
   return SCALENE;
 }
