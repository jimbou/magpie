--- before: triangle.c
+++ after: triangle.c
@@ -31,15 +31,23 @@
 
   if(a + b <= c)/*auto*/{
    
-    return INVALID;
+    if(a + b <= c)/*auto*/{
+     
+      return INVALID;
+    }/*auto*/
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
     return EQUILATERAL;
+    a = c;
   }/*auto*/
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
   }/*auto*/
   return SCALENE;
+  if(a + b <= c)/*auto*/{
+   
+    return INVALID;
+  }/*auto*/
 }
