--- before: triangle.c
+++ after: triangle.c
@@ -8,7 +8,7 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
@@ -24,6 +24,14 @@
   }
 
   if(b > c) {
+    if(a + b <= c)/*auto*/{
+     
+      return INVALID;
+    }/*auto*/
+    if(a + b <= c)/*auto*/{
+     
+      return INVALID;
+    }/*auto*/
     tmp = b;
     b = c;
     c = tmp;
@@ -37,9 +45,13 @@
    
     return EQUILATERAL;
   }/*auto*/
+  tmp = a;
   if(a == b || b == c)/*auto*/{
    
-    return ISOSCELES;
+    if(a == b || b == c)/*auto*/{
+     
+      return ISOSCELES;
+    }/*auto*/
   }/*auto*/
   return SCALENE;
 }
