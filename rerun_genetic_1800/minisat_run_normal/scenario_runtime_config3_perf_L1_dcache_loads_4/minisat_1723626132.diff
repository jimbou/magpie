--- before: core/Solver.cc
+++ after: core/Solver.cc
@@ -639,7 +639,7 @@
             }/*auto*/
         else
             /*auto*/{
-                cs[j++] = cs[i];
+                
             }/*auto*/
     }
     cs.shrink(i - j);
@@ -892,6 +892,7 @@
         }/*auto*/
         curr_restarts++;
     }
+    Lit            p   = trail[qhead++];
 
     if (verbosity >= 1)
         /*auto*/{
