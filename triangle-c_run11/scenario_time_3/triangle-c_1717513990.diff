--- before: triangle.c
+++ after: triangle.c
@@ -8,7 +8,7 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
@@ -17,11 +17,10 @@
     b = tmp;
   }
 
-  if(a > c) {
-    tmp = a;
-    a = c;
-    c = tmp;
-  }
+  if(a == b && b == c)/*auto*/{
+   
+    return EQUILATERAL;
+  }/*auto*/
 
   if(b > c) {
     tmp = b;
@@ -33,10 +32,7 @@
    
     return INVALID;
   }/*auto*/
-  if(a == b && b == c)/*auto*/{
-   
-    return EQUILATERAL;
-  }/*auto*/
+  
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
