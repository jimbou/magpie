--- before: triangle.c
+++ after: triangle.c
@@ -8,7 +8,11 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  if(b > c) {
+    tmp = b;
+    b = c;
+    c = tmp;
+  }
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
