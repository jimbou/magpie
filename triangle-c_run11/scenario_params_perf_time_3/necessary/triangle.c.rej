--- before: triangle.c
+++ after: triangle.c
@@ -40,6 +40,7 @@
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
+    const struct timespec ms = {0, 0.001*1e9};
   }/*auto*/
   return SCALENE;
 }
