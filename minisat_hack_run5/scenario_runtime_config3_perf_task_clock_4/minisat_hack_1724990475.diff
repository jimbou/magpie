--- before: sources/core/Solver.cc
+++ after: sources/core/Solver.cc
@@ -515,6 +515,7 @@
                 }
             }
         }
+        vec<Var> vs;
     }
 
     return true;
@@ -703,7 +704,7 @@
         Clause& c = ca[cs[i]];
         if (satisfied(c))/*auto*/{
             
-            removeClause(cs[i]);
+            
         }/*auto*/
         else/*auto*/{
             
