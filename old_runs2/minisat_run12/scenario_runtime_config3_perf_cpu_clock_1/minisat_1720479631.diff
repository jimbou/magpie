--- before: core/Solver.cc
+++ after: core/Solver.cc
@@ -474,7 +474,7 @@
             return;
         }/*auto*/
 
-    seen[var(p)] = 1;
+    
 
     for (int i = trail.size()-1; i >= trail_lim[0]; i--){
         Var x = var(trail[i]);
