--- before: triangle.c
+++ after: triangle.c
@@ -36,7 +41,10 @@
 
   if(a + b <= c)/*auto*/{
    
-    return INVALID;
+    if(a + b <= c)/*auto*/{
+     
+      return INVALID;
+    }/*auto*/
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
