--- before: triangle.c
+++ after: triangle.c
@@ -32,6 +32,10 @@
   if(a + b <= c)/*auto*/{
    
     return INVALID;
+    if(a == b && b == c)/*auto*/{
+     
+      return EQUILATERAL;
+    }/*auto*/
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
