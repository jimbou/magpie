--- before: triangle.cpp
+++ after: triangle.cpp
@@ -8,7 +8,7 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  tmp = a;
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
@@ -22,6 +22,7 @@
     a = c;
     c = tmp;
   }
+  tmp = a;
 
   if(b > c) {
     tmp = b;
@@ -35,6 +36,11 @@
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
+    if(a > c) {
+      tmp = a;
+      a = c;
+      c = tmp;
+    }
     return EQUILATERAL;
   }/*auto*/
   if(a == b || b == c)/*auto*/{
