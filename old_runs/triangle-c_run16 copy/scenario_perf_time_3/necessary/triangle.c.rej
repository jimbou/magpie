--- before: triangle.c
+++ after: triangle.c
@@ -27,11 +27,13 @@
     tmp = b;
     b = c;
     c = tmp;
+    
   }
 
   if(a + b <= c)/*auto*/{
    
     return INVALID;
+    return ISOSCELES;
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
