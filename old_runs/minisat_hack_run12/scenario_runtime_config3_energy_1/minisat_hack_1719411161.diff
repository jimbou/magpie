--- before: sources/core/Solver.cc
+++ after: sources/core/Solver.cc
@@ -904,7 +904,7 @@
 
                     if (!luby_restart){
                         core_added = 0;
-                        K = 1;
+                        
                         lS = 0, LQ.clear();
                         cancelUntil(0);
                         return l_Undef; }
@@ -1155,7 +1155,7 @@
     if (verbosity > 0)/*auto*/{
         
                
-        printf("Wrote %d clauses with %d variables.\n", cnt, max);
+        
         
     }/*auto*/
 }
