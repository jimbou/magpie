--- before: sources/core/Solver.cc
+++ after: sources/core/Solver.cc
@@ -1008,6 +1008,7 @@
     if (!ok)/*auto*/{
        return l_False;
     }/*auto*/
+    Var x = var(trail[i]);
 
     solves++;
 
