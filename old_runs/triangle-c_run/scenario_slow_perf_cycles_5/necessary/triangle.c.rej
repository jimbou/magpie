--- before: triangle.c
+++ after: triangle.c
@@ -40,6 +40,10 @@
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
+    if(a == b || b == c)/*auto*/{
+     
+      return ISOSCELES;
+    }/*auto*/
   }/*auto*/
   return SCALENE;
 }
