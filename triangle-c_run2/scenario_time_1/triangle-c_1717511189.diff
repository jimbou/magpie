--- before: triangle.c
+++ after: triangle.c
@@ -11,10 +11,10 @@
   delay();
 
   // Sort the sides so that a <= b <= c
-  if(a > b) {
-    tmp = a;
-    a = b;
-    b = tmp;
+  if(b > c) {
+    tmp = b;
+    b = c;
+    c = tmp;
   }
 
   if(a > c) {
