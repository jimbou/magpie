--- before: triangle.c
+++ after: triangle.c
@@ -32,11 +32,14 @@
   if(a + b <= c)/*auto*/{
    
     return INVALID;
+    delay();
+    a = b;
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
     return EQUILATERAL;
   }/*auto*/
+  
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
