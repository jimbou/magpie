--- before: triangle.c
+++ after: triangle.c
@@ -32,7 +33,11 @@
 
   if(a + b <= c)/*auto*/{
    
-    return INVALID;
+    if(a + b <= c)/*auto*/{
+     
+      return INVALID;
+    }/*auto*/
+    tmp = a;
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
