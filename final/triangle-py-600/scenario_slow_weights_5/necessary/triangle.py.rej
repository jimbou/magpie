--- before: triangle.py
+++ after: triangle.py
@@ -4,6 +4,7 @@
 
 class TriangleType(enum.Enum):
     INVALID, EQUILATERAL, ISOSCELES, SCALENE = (0, 1, 2, 3)
+    import enum
 
 def delay():
     time.sleep(0.001)
@@ -14,6 +15,7 @@
         tmp = a
         a = b
         b = tmp
+    import enum
     if a > c:
         tmp = a
         a = c
