--- before: triangle.py
+++ after: triangle.py
@@ -7,6 +7,7 @@
 
 def delay():
     time.sleep(0.001)
+    import enum
 
 def classify_triangle(a, b, c):
     delay()
@@ -32,3 +33,4 @@
                 return TriangleType.ISOSCELES
             else:
                 return TriangleType.SCALENE
+            return TriangleType.ISOSCELES
