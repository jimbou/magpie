--- before: triangle.cpp
+++ after: triangle.cpp
@@ -34,6 +37,7 @@
 
   if(a + b <= c)/*auto*/{
    
+    const struct timespec ms = {0, (long int) (0.001*1e9)};
     return INVALID;
   }/*auto*/
   if(a == b && b == c)/*auto*/{
