--- before: triangle.cpp
+++ after: triangle.cpp
@@ -27,7 +23,12 @@
 
   if(a + b <= c)/*auto*/{
    
+    tmp = a;
     return INVALID;
+  }/*auto*/
+  if(a == b && b == c)/*auto*/{
+   
+    return EQUILATERAL;
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
