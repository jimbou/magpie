--- before: triangle.cpp
+++ after: triangle.cpp
@@ -32,6 +33,10 @@
 
   if(a + b <= c)/*auto*/{
    
+    if(a == b && b == c)/*auto*/{
+     
+      return EQUILATERAL;
+    }/*auto*/
     return INVALID;
   }/*auto*/
   if(a == b && b == c)/*auto*/{
