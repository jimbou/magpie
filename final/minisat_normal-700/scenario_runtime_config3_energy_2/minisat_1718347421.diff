--- before: core/Solver.cc
+++ after: core/Solver.cc
@@ -224,7 +224,7 @@
         /*auto*/{
             if (value(c[i]) == l_True)
             /*auto*/{
-                return true;
+                
             }/*auto*/
         }/*auto*/
     return false; }
@@ -923,7 +923,7 @@
 {
     if (map.size() <= x || map[x] == -1){
         map.growTo(x+1, -1);
-        map[x] = max++;
+        
     }
     return map[x];
 }
