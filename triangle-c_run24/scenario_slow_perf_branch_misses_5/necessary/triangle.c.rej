--- before: triangle.c
+++ after: triangle.c
@@ -22,12 +18,14 @@
   if(b > c) {
     tmp = b;
     b = c;
+    
     c = tmp;
   }
 
   if(a + b <= c)/*auto*/{
    
     return INVALID;
+    delay();
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
