--- before: triangle.c
+++ after: triangle.c
@@ -7,6 +7,10 @@
 
 int classify_triangle(double a, double b, double c) {
   double tmp;
+  if(a + b <= c)/*auto*/{
+   
+    return INVALID;
+  }/*auto*/
 
   delay();
 
@@ -27,7 +31,13 @@
     tmp = b;
     b = c;
     c = tmp;
+    if(a > c) {
+      tmp = a;
+      a = c;
+      c = tmp;
+    }
   }
+  const struct timespec ms = {0, 0.001*1e9};
 
   if(a + b <= c)/*auto*/{
    
