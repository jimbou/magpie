--- before: core/Solver.cc
+++ after: core/Solver.cc
@@ -892,6 +892,7 @@
         }/*auto*/
         curr_restarts++;
     }
+    Lit            p   = trail[qhead++];
 
     if (verbosity >= 1)
         /*auto*/{
