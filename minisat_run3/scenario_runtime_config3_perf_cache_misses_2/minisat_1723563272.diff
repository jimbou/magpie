--- before: core/Solver.cc
+++ after: core/Solver.cc
@@ -932,6 +932,8 @@
 void Solver::toDimacs(FILE* f, Clause& c, vec<Var>& map, Var& max)
 {
     if (satisfied(c)) /*auto*/{
+      watches.smudge(~c[1]);
+      watches.smudge(~c[1]);
       return;
     }/*auto*/
 
