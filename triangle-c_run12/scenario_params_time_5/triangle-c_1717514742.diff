--- before: triangle.c
+++ after: triangle.c
@@ -8,7 +8,7 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
@@ -35,6 +35,7 @@
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
+    b = tmp;
     return EQUILATERAL;
   }/*auto*/
   if(a == b || b == c)/*auto*/{
