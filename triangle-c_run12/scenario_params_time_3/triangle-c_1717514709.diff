--- before: triangle.c
+++ after: triangle.c
@@ -8,15 +8,16 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  
 
   // Sort the sides so that a <= b <= c
-  if(a > b) {
+  
+
+  if(a > c) {
     tmp = a;
-    a = b;
-    b = tmp;
+    a = c;
+    c = tmp;
   }
-
   if(a > c) {
     tmp = a;
     a = c;
@@ -36,6 +37,7 @@
   if(a == b && b == c)/*auto*/{
    
     return EQUILATERAL;
+    tmp = a;
   }/*auto*/
   if(a == b || b == c)/*auto*/{
    
