--- before: triangle.c
+++ after: triangle.c
@@ -8,7 +8,11 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  if(a > b) {
+    tmp = a;
+    a = b;
+    b = tmp;
+  }
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
@@ -21,6 +25,7 @@
     tmp = a;
     a = c;
     c = tmp;
+    
   }
 
   if(b > c) {
@@ -35,6 +40,11 @@
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
+    if(b > c) {
+      tmp = b;
+      b = c;
+      c = tmp;
+    }
     return EQUILATERAL;
   }/*auto*/
   if(a == b || b == c)/*auto*/{
