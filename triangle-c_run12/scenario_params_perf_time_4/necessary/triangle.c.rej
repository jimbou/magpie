--- before: triangle.c
+++ after: triangle.c
@@ -40,6 +45,11 @@
   }/*auto*/
   if(a == b && b == c)/*auto*/{
    
+    if(b > c) {
+      tmp = b;
+      b = c;
+      c = tmp;
+    }
     return EQUILATERAL;
   }/*auto*/
   if(a == b || b == c)/*auto*/{
