--- before: triangle.c
+++ after: triangle.c
@@ -8,7 +8,7 @@
 int classify_triangle(double a, double b, double c) {
   double tmp;
 
-  delay();
+  
 
   // Sort the sides so that a <= b <= c
   if(a > b) {
@@ -37,9 +37,14 @@
    
     return EQUILATERAL;
   }/*auto*/
+  if(a == b && b == c)/*auto*/{
+   
+    return EQUILATERAL;
+  }/*auto*/
   if(a == b || b == c)/*auto*/{
    
     return ISOSCELES;
+    delay();
   }/*auto*/
   return SCALENE;
 }
